(function(window) {

    var plat = {

        // TODO: build task to sync w package.json
        VERSION: '0.0.1',

        Models: {},
        models: {},

        Views: {},
        views: {},

        Workers: {},
        workers: {}
    };

    // TODO: build task to create AMD version, min, concat, etc
    window.plat = plat;
} (this));